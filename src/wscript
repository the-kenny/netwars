#! /usr/bin/env python
# encoding: utf-8
# Thomas Nagy, 2006 (ita)

import sys, os
import Utils, Options

def set_options(opt):
	pass

def configure(conf):
	conf.sub_config('game')
	#conf.sub_config('gui')
	conf.sub_config('lib')
	return
	
def build(bld):
	bld.add_subdirs('game')
	#bld.add_subdirs('gui')
	conf.add_subdirs('lib')


